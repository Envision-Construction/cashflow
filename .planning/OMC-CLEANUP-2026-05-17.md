# OMC Cleanup — cashflow

**Date:** 2026-05-17
**Master ref:** `5d94cb0` in claude-code-memory
**Trigger:** oh-my-claudecode uninstalled 2026-05-17; cleanup of all OMC traces across portfolio.

## Hits (pre-cleanup)

Grep scan against `oh-my-claudecode|.omc/|OMC_QUIET|omc-cache-sync|omc-auto-update|omc-hud` over tracked source files (excluding caches, backups, node_modules, .git):

| Path | Refs | Notes |
|---|---|---|
| `.planning/codebase/STRUCTURE.md` | 2 lines (L14 dir listing, L144-148 "Special Directories" block) | Auto-generated by GSD codebase mapper; references `.omc/` as "OMC orchestration state" |

Untracked filesystem residue (not in grep scope but to be removed):

| Path | Size | Notes |
|---|---|---|
| `.omc/` (dir) | 12K | 3 files: `project-memory.json`, `state/mission-state.json`, `sessions/c1209cf5-….json`. Not in git (untracked per `git status`). Last touched 2026-04-03. |

No hits in: `*.json` config, `*.sh`, `*.mjs`, `*.js`, `*.ts`, `*.py`, `*.yaml`, `*.yml`. No hook scripts, no env vars, no branding.

## Changes

1. **`.planning/codebase/STRUCTURE.md`** — strip `.omc/` from directory layout block; remove the "`.omc/`:" Special Directories subsection entirely (the dot-prefix naming bullet keeps its other examples).
2. **`.omc/` directory** — delete in full (untracked, never committed, holds dead OMC orchestration state). Confirms with master cleanup protocol where `.omc/` → `.claude/` mapping applies only if state needs preserving; cashflow's `.omc/` is stale orchestration debris, not active project memory.

No `OMC_QUIET`, `omc-cache-sync`, `omc-auto-update`, or `omc-hud` hooks present → nothing to deregister.

## Verification

```
cd ~/GitHub/cashflow
grep -rln -iE 'oh-my-claudecode|\.omc/|OMC_QUIET|omc-cache-sync|omc-auto-update|omc-hud' \
  --include='*.md' --include='*.json' --include='*.sh' --include='*.mjs' \
  --include='*.js' --include='*.ts' --include='*.py' --include='*.yaml' --include='*.yml' . 2>/dev/null \
  | grep -v 'node_modules\|\.git/\|gsd-user-files-backup\|\.omc\.archive\|\.bak\|\.pre-ccm\|red-team\|runtime/plugins/cache'
# expected: empty

ls -la .omc/ 2>&1
# expected: No such file or directory
```
